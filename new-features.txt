* In the stats section on the lower right of the page, next to total modules, add a view for total items

* Consumable items
** DB - items table add column for consumable
** FrontEnd - Add checkbox on item pages for consumable

* On the modules page, the sort by filter is not working 

* On the modules page, move the Season Column to the far left

* When adding modules, make sure APL value is only between 1-20

* Item additions, need validation to ensure no exact duplicates get added, there needs to be some difference (i.e. unique name or property)

* On the items list display, add a column showing the modules codes where that item is a reward

* Item Display and Filtering
** DB - items table add column for rarity (string) and attunement (boolean)
** FrontEnd - Add fields for rarity and attunement with corresponding filters

* Module Setting
** DB - modules table add a column for setting (FR - Forgotten Realms, RV - Ravenloft, etc.)
** FrontEnd - On the modules display add a column for the module setting and corresponding filters, when adding the modules the setting should be a check box picker because modules can be part of more than one setting, epics and planescape for example
